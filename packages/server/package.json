{
  "name": "@come/server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "db:init-local": "wrangler d1 execute db_come --local --file=./db_schema/create_tables.sql"
  },
  "dependencies": {
    "hono": "^4.6.20",
    "@come/common": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.13.0",
    "@types/service-worker-mock": "^2.0.4",
    "wrangler": "^4.26.0"
  }
}